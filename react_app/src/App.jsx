import React from 'react';
import FakeSystem from './FakeSystem.jsx';

/**
 * 应用根：模拟函证系统制函管理外壳。
 * 真实可交互页面：制函管理 → Excel 制函（单页批量制函工作台）。
 * 其余模块仅做展示（占位）。
 */
export default function App() {
  return <FakeSystem />;
}
